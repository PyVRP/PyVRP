# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

PyVRP is a state-of-the-art vehicle routing problem (VRP) solver. It uses a hybrid architecture: performance-critical parts are implemented in C++20, while the high-level algorithm and customization interface are in Python.

The package supports various VRP variants including CVRP (capacitated), VRPTW (with time windows), multi-depot, pickups/deliveries, heterogeneous fleets, optional clients, and more.

**New features**:
- Elevation cost: Locations have an optional `elevation` parameter (meters). Routes accumulate `load × elevation_gain` over each arc (positive climb only), weighted by `VehicleType(unit_elevation_cost=...)`. Use `Model.add_location(x, y, elevation=…)` to set per-location elevation; `Client` and `Depot` reference a location by index. See `tests/test_elevation_minimal.py` for unit tests.

## Build System

```bash
uv sync                                                  # install dependencies
uv run buildtools/build_extensions.py                    # build C++ extensions (release)
uv run buildtools/build_extensions.py --build_type debug --clean  # debug build
uv run pytest                                            # run tests
uv run pre-commit run --all-files                        # lint and format
```

## Architecture

For a detailed walkthrough of the algorithm and software design, see
[SOLVER_ARCHITECTURE.md](SOLVER_ARCHITECTURE.md).

### Python/C++ split

Performance-critical work (local search operators, route cost tracking, delta
evaluation) lives in `pyvrp/cpp/` and is compiled into `_pyvrp.so` and `_search.so`.
The outer algorithm loop (`IteratedLocalSearch`), penalty management, and configuration
are in Python (`pyvrp/*.py`).

Each C++ subdirectory has a `bindings.cpp` that exposes types to Python via pybind11,
and `*_docs.h` files with auto-generated docstrings (generated by Meson at build time).

### Data flow

1. User defines problem using `Model` API → `Model.data()` creates `ProblemData`
2. `solve()` constructs `LocalSearch`, `PenaltyManager`, `IteratedLocalSearch`
3. `IteratedLocalSearch.run()` iterates: perturb → local search → LAHC acceptance → penalty update
4. Returns `Result` with the best solution found and statistics

## Documentation

See `docs/CLAUDE.md` for a full index with per-page summaries. Most relevant during development:
- `dev/algorithm.rst` — ILS pseudocode and academic references
- `dev/new_vrp_variants.rst` — step-by-step guide for adding new VRP variants (prize-collecting as worked example)
- `dev/contributing.rst` — build setup, debugging mixed Python/C++ with VSCode, profiling with perf
- `dev/glossary.rst` — definitions of concatenation scheme, penalised cost, route segment
- `api/pyvrp.rst` and `api/search.rst` — full API reference for core types and operators

## Development Notes

### When adding a new operator:

1. Create `.h` and `.cpp` files in `pyvrp/cpp/search/`
2. Inherit from `UnaryOperator` or `BinaryOperator`
3. Implement required methods (`evaluate`, `apply`, `supports`)
4. Add bindings in `pyvrp/cpp/search/bindings.cpp`
5. Export in `pyvrp/search/__init__.py`
6. Optionally add to `OPERATORS` list for default use

### When adding a new cost component:

**Two Route classes**: PyVRP has `pyvrp::Route` (full statistics) and `pyvrp::search::Route` (fast search). New cost methods must be added to both.

**CostEvaluatable concept**: Add method signature to `CostEvaluatable` concept in `CostEvaluator.h`:
```cpp
concept CostEvaluatable = requires(T arg) {
    { arg.newCost() } -> std::same_as<Cost>;
};
```

**Implementation order**:
1. Add field/getter to `Route` and `Solution` (in `pyvrp/cpp/`)
2. Compute in `Route` constructor (section between distance and load statistics)
3. Add stub to `search::Route` (often `return 0;`)
4. Update `CostEvaluator::penalisedCost()` to include new cost
5. Update bindings: expose getter, update pickle with `t.size() > N` pattern for backward compatibility
6. For performance: implement delta evaluation in `search::Route::Proposal`

**Location access**: Use `data.location(idx)` which handles both depots and clients via union type. Client indices are offset by `numDepots()`.

**When modifying C++ code**: update `bindings.cpp` if the public API changed, then rebuild with `uv run buildtools/build_extensions.py --clean`.
