name: CD

on:
  release:
    types: [ created ]
  pull_request:  # uncomment these lines to test CD build
    branches: [ main ]

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  linux:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-2019]  # [ubuntu-20.04, windows-2019, macos-10.15]
    steps:
      - uses: actions/checkout@v3
      - name: Install rtools
        if: ${{ runner.os == 'Windows' }}
        run: |
          # mingw-w64
          choco install rtools --no-progress
          echo "c:\rtools40\ucrt64\bin;" >> $env:GITHUB_PATH
      - name: Build wheels
        uses: pypa/cibuildwheel@v2.12.0
        with:
          package-dir: .
          output-dir: dist
      - uses: actions/upload-artifact@v3  # upload all wheels
        with:
          name: dist
          path: ./dist/*

  # deploy:
  #   name: Deploy to PyPI
  #   needs: [ linux ]  # , windows, macos ]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #     - name: Set up Python
  #       uses: actions/setup-python@v4
  #       with:
  #         python-version: 3.9
  #     - name: Update pip and poetry
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install poetry
  #     - uses: actions/download-artifact@v3  # download previously built wheels
  #       with:
  #         name: dist
  #         path: dist
  #     - name: Build sdist
  #       run: poetry build --format sdist
  #     - name: Check packaged distributions
  #       run: ls -la dist
  #     - name: Deploy to PyPI
  #       run: |
  #         poetry config pypi-token.pypi ${{ secrets.PYPI_TOKEN }}
  #         poetry publish
